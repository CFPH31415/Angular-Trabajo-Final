// Esta es una interface de Producto, que define los atributos de un objeto producto.
export interface Producto{
    // Incluye un ID de tipo string, un nombre de producto de tipo string, un costo de tipo número y una foto de tipo string.
    id: string;
    producto: string;
    costo: number;
    foto:string;
}
